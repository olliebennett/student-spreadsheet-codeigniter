this is inside the admin_view.php file.<?php function render_table($arr,$id) { ?>		<table id="<?php echo $id; ?>">		<thead>			<tr>				<?php foreach (array_keys($arr[0]) as $row_id) {				echo '<th>'.ucwords(str_replace('_',' ',$row_id)).'</th>';				//echo '<th>'.$row_id.'</th>';				} ?>			</tr>		</thead>		<tbody>	<?php foreach ($arr as $row) : ?>			<tr>				<?php foreach ($row as $k => $v) {				echo '<td>'.$v.'</td>';				} ?>			</tr>	<?php endforeach; ?>		</tbody>	</table>		<?php } // END render_table() ?><?php // Show relevant data		foreach (array('houses','users','log') as $tabletype) {		if(isset($$tabletype)) render_table($$tabletype,$tabletype);	}